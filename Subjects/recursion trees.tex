\section{Recursion Trees}
The height of a recursion tree is the logarithm with base equal to the factor by which the problem size is divided.

The number of children in a specific layer is the branching factor raised to the level index.
\begin{figure}[H]
    \centering
    \includegraphics[width=1\linewidth]{Images/Recursion Tree.png}
\end{figure}
